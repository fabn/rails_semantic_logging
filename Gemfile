source 'https://rubygems.org'

gemspec

gem 'rails', '>= 7.2'

gem 'appraisal', '~> 2.5'
gem 'rspec-rails', '~> 6.0'
gem 'rubocop', '~> 1.50'
gem 'rubocop-performance'
gem 'rubocop-rails'
gem 'rubocop-rspec'
gem 'rubocop-rspec_rails'

# Development-only: the Puma integration is duck-typed, so the gem is not a
# runtime dependency — it is needed to drive Puma::LogWriter in the specs.
gem 'puma', '>= 6.2'
